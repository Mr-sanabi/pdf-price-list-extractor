# PDF Price List Extractor

A learning project for extracting structured product data from PDF price lists.

## Planned pipeline

PDF → extract → parse → normalize → validate → export

The project is currently in the setup and exploration stage.
